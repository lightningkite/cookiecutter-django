machine:
  timezone:
    America/Denver

  python:
    version:
      3.5.3

dependencies:
  pre:
    - pip install -r requirements.txt

test:
  override:
    - pytest
    - rm -rf project_name
    - yes "" | cookiecutter .
    - python project_name/manage.py test